# workflow name
name : First Workflow

# Event at which the workflow to be triggered
on  :  
  push: 
  workflow_dispatch:
jobs: 

  Hello-job:
  
    # runner
    runs-on: ubuntu-latest
    steps:
      - name : Hello World
        run : echo "Hello World"

      - name : Multi-line bash
        run : 
            echo  "I am" 
            echo  " a multi-line" 
            echo  " script" 

  echo-GoodBy:
    runs-on: ubuntu-slim
    steps:
      - name : Say GoodBye
        run : echo "GoodBye!:"          
      - name: Event Name
        run :
           echo "Event name: ${{github.event-name}}"  #Context variable
