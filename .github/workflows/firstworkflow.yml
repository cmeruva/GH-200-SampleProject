# workflow name
name : First Workflow

# Event at which the workflow to be triggered
on  :  
  push: 

jobs: 

  Hello-job:
  # runner
  runs-on: ubuntu-latest
  steps:
    - name : Hello World
      run : echo "Hello World"
